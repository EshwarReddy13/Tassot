specVersion: "v1"
serviceId: "project-management"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "project-management-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
